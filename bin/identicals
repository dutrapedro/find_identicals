#!/usr/bin/env ruby

require 'find_identicals'

identicals = FindIdenticals.new(ARGV.first).identicals

if identicals.empty?
  puts "Any duplicate ID's found!"
else
  puts "The following ID's are identical:"
  identicals.each { | id | puts "\t- ##{id}" }
end